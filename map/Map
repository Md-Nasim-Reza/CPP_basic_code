#include<bits/stdc++.h>
using namespace std;
int main(){
    int i,n,a;
    string s;
    map<string,int>mp;
    cin>>n;
    for(i=0; i<n; i++){
        cin>>s;
        cin>>a;
        mp[s] = a;
    }
     //cout<<mp[2];
    for(auto it : mp){
        cout<<it.first<<' '<<it.second<<endl;
    }
return 0;
}
