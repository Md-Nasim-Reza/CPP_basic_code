#include<bits/stdc++.h>
using namespace std;
main(){
    int i,j,m,n,a;
    vector<int>v[100];
    cin>>n;
    for(i=0; i<n; i++){
        cin>>m;
        for(j=0; j<m; j++){
            cin>>a;
            v[i].push_back(a);
        }
    }
    for(i=0; i<n; i++){
        for(j=0; j<v[i].size(); j++)
            printf("%d ", v[i][j]);
    printf("\n");
    }
    return 0;
}
